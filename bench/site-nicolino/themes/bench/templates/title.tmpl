{#
  templates/title.tmpl - Minimal page title (no breadcrumbs for cleaner look)

  Renders:
  - Simple page title as a link
  - Taxonomy links (tags, categories, etc.) if present

  Variables:
  - link: URL for the page title
  - title: Page title text
  - taxonomies: Hash of taxonomy terms (tags, categories, etc.)

  Used by: post.tmpl, gallery.tmpl, folder_index feature, and manually in code
#}
<h1 class="primary"><a href="{{link}}">{{title}}</a></h1>

{% if taxonomies | length > 1 %}
<nav class="taxonomies">
  <ul>
    {% for k,v in taxonomies %}
    <li>
      {{k | link }} :
      {% for term in v %}
      {{term | link}}
      {% endfor%}
    </li>
    {% endfor %}
  </ul>
</nav>
{% endif %}
